<div id="sticker" class="show-nav">
  <a href="#top">
    <div class="back-top">
      <p class="wide">BACK TO TOP  <i class="fa fa-arrow-up" aria-hidden="true"></i></p>
    </div>
  </a>
  <a href="#map-container">
    <div class="show-nav-link">
      <p class="wide">MAP + PLACES</p>
    </div>
  </a>
  <a href="#instagram-block">
    <div class="show-nav-link">
      <p class="wide">GALLERY</p>
    </div>
  </a>
  <a href="#show-cards">
    <div class="show-nav-link">
      <p class="wide">SIMILAR DISTRIKTS</p>
    </div>
  </a>

</div>

<% content_for :after_js do %>
<script>
  $(document).ready(function(){
    // Add smooth scrolling to all links

    $(".show-nav a").on('click', function(event) {
      var self = this;
      // Make sure this.hash has a value before overriding default behavior
      if (this.hash !== "") {
        // Prevent default anchor click behavior
        event.preventDefault();

        // Store hash
        var hash = this.hash;

        // Using jQuery's animate() method to add smooth page scroll
        // The optional number (800) specifies the number of milliseconds it takes to scroll to the specified area
        $('html, body').animate({
          scrollTop: $(hash).offset().top - 46
        }, 600, function(){
          $(".show-nav .show-nav-link").removeClass('active');
          $(self).find('.show-nav-link').addClass('active');
          // Add hash (#) to URL when done scrolling (default click behavior)
//          window.location.hash = hash;
          window.history.replaceState("", document.title, window.location.href.replace(window.location.hash, "") + hash);
        });
      } // End if
    });
  });
</script>
<% end %>
